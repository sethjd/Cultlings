// Optional Firebase configuration.
//
// Leave this as null to run Cultlings entirely offline with localStorage and
// mock multiplayer data. To enable Firebase, copy the object shape from
// firebaseConfig.example.js and replace the placeholder strings with the
// configuration shown in your Firebase project's Web App settings.
window.CULTLINGS_FIREBASE_CONFIG = null;
